$kind: http-request
url: http://localhost:3000/api/v1/auth/logout
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token}}"
scripts:
  - type: afterResponse
    code: pm.environment.unset("token");
    language: text/javascript
order: 5000
