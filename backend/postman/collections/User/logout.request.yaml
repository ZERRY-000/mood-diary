$kind: http-request
name: logout
url: "{{baseURL}}/api/v1/auth/logout"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token}}"
scripts:
  - type: afterResponse
    code: pm.environment.unset("token");
    language: text/javascript
order: 5000
