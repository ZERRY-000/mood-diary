name: delete-activity
$kind: http-request
description: Delete activity code from scratch 
url: "{{baseURL}}/api/v1/activities/{{recentActivityId}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token}}"
scripts:
  - type: afterResponse
    code: |
      pm.execution.setNextRequest(null) 
      console.log("hello world")
    language: text/javascript
  - type: beforeRequest
    code: |
      pm.execution.setNextRequest(null) 
      console.log("hello world")
    language: text/javascript
order: 6000
